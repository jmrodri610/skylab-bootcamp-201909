function Feedback ({message}) {
    return <section>
        <span><i className="feedback__icon">🤡</i></span>
        <p className="feedback__message">{message}</p>
        <span className="feedback__icon">🎈</span>
    </section>
}